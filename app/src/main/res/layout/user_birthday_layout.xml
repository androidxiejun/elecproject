<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="10dp"
    android:background="#00FFFFFF">

    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:padding="10dp">

        <DatePicker
            android:id="@+id/user_brithday_date"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:datePickerMode="spinner"
            android:background="#FFFFFF"
            android:calendarTextColor="#FFFFFF"
            android:calendarViewShown="false"/> <!--不显示日历样式 -->

        <TextView
            android:id="@+id/user_birthday_ensure"
            android:layout_width="match_parent"
            android:layout_height="40dp"
            android:layout_below="@id/user_brithday_date"
            android:background="@drawable/user_nick"
            android:textColor="#000000"
            android:gravity="center"
            android:textSize="16sp"
            android:text="确定"/>

    </RelativeLayout>

</RelativeLayout>